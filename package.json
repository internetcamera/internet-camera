{
  "name": "internet-camera",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "chain": "cd packages/contracts && yarn chain",
    "website": "cd packages/website && yarn dev",
    "sdk": "cd packages/sdk && yarn dev",
    "ipfs-node": "cd packages/ipfs-node && yarn dev",
    "tx": "cd packages/tx && yarn dev"
  }
}
