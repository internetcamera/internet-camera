{
  "name": "subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ shahruz/ic-mumbai-one",
    "create-local": "graph create --node http://localhost:8020/ internetcamera/subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ internetcamera/subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 internetcamera/subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.20.1",
    "@graphprotocol/graph-ts": "0.20.0"
  },
  "private": "true",
  "version": "0.0.69"
}
